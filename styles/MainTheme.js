import { createTheme } from "@mui/material/styles";

const palette = {
  tindota: {
    primary: "#9f2812",
    secondary: "#ac2e15",
    tertiary: "#000301",
    titleText: "#e2dace",
    mainText: "#fff",
    primaryBg: "#1c242d",
    secondaryBg: "#28333d",
    tertiaryBg: "#2d3741",
  },
};

export default createTheme({
  palette: palette,
});
